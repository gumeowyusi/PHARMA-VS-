server.port=8080

# Load secrets từ file riêng (gitignored) - xem application-secret.properties.example
spring.config.import=optional:classpath:application-secret.properties

# Database connection
spring.datasource.url=jdbc:sqlserver://${DB_HOST:localhost}:${DB_PORT:1433};databaseName=${DB_NAME:STORE};encrypt=${DB_ENCRYPT:true};trustServerCertificate=${DB_TRUST_CERT:true};
spring.datasource.username=${DB_USERNAME:sa}
spring.datasource.password=${DB_PASSWORD:Nhanhtam456}

# JPA settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.format_sql=true

# SMTP
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=capyboy.dev@gmail.com
spring.mail.password=thzh bvmp ttgm azpm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000

# PayOS credentials (override in application-secret.properties or env vars)
PAYOS_CLIENT_ID=cbec4837-fa2f-4791-9d75-e0dde7c47cb5
PAYOS_API_KEY=e1c07c6c-b85c-49fa-bdd2-f0e7cb31a43c
PAYOS_CHECKSUM_KEY=8c0f6f5e574151dcc1e8c4b88f48f272286b7193813733da1e9e728ecdd31cfd

# Gemini AI (override in application-secret.properties or env vars)
GEMINI_API_KEY=
GEMINI_MODEL_ID=gemini-2.5-flash

# Thymeleaf
spring.thymeleaf.cache=false

# File upload limits (increase to allow large product images)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=25MB
server.tomcat.max-http-form-post-size=25MB

# Payment QR config
payment.bank.bin=970422
payment.bank.account=060201032406
payment.bank.name=TU GIA TUYEN
payment.bank.bank-name=MB Bank
payment.momo.phone=0353048599
payment.momo.name=TU GIA TUYEN
payment.zalopay.phone=0353048599
payment.zalopay.name=TU GIA TUYEN

# OAuth2 Google
# Điền client-id / client-secret vào application-secret.properties (xem file .example)
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# Facebook login đã tắt – chỉ dùng Google OAuth2

# Scheduling
spring.task.scheduling.pool.size=2

# Logging
spring.messages.encoding=UTF-8
logging.level.org.springframework=INFO
logging.level.org.springframework.mail=DEBUG
logging.level.com.sun.mail=DEBUG
